import requests
import json
import time
import os
import base64
from datetime import date, timedelta
from flask import Flask


TELEGRAM_BOT_TOKEN = os.environ.get("TELEGRAM_BOT_TOKEN")
MESHY_API_KEY = os.environ.get("MESHY_API_KEY")


if not TELEGRAM_BOT_TOKEN:
    raise ValueError("TELEGRAM_BOT_TOKEN not set")
if not MESHY_API_KEY:
    raise ValueError("MESHY_API_KEY not set")


app = Flask(__name__)


API_URL = f"https://api.telegram.org/bot{TELEGRAM_BOT_TOKEN}"
MESHY_API_URL = "https://api.meshy.ai/openapi/v2"
USAGE_FILE = "user_usage.json"
user_states = {}


print("🟢 Bot starting...")


# ========== ЛИМИТЫ ==========
def load_usage():
    if os.path.exists(USAGE_FILE):
        try:
            with open(USAGE_FILE, 'r') as f:
                return json.load(f)
        except:
            return {}
    return {}


def save_usage(usage):
    try:
        with open(USAGE_FILE, 'w') as f:
            json.dump(usage, f)
    except:
        pass


def get_user(user_id):
    usage = load_usage()
    uid = str(user_id)
    today = date.today().isoformat()
    if uid not in usage:
        return "free", 3
    if usage[uid].get("premium_until", "") >= today:
        return "premium", float('inf')
    if usage[uid].get("date") == today:
        return "free", max(0, 3 - usage[uid].get("count", 0))
    return "free", 3


def decrement(user_id):
    usage = load_usage()
    uid = str(user_id)
    today = date.today().isoformat()
    if uid not in usage or usage[uid].get("date") != today:
        usage[uid] = {"date": today, "count": 1}
    else:
        usage[uid]["count"] = usage[uid].get("count", 0) + 1
    save_usage(usage)


# ========== ОТПРАВКА ==========
def send_message(chat_id, text, keyboard=None):
    url = API_URL + "/sendMessage"
    payload = {"chat_id": chat_id, "text": text, "parse_mode": "HTML"}
    if keyboard:
        payload["reply_markup"] = json.dumps(keyboard)
    try:
        requests.post(url, json=payload, timeout=10)
    except Exception as e:
        print(f"send_message error: {e}")


def send_document(chat_id, file_bytes, filename, caption=""):
    url = API_URL + "/sendDocument"
    files = {"document": (filename, file_bytes)}
    data = {"chat_id": chat_id, "caption": caption}
    try:
        requests.post(url, files=files, data=data, timeout=60)
    except Exception as e:
        print(f"send_document error: {e}")


# ========== КЛАВИАТУРЫ ==========
def main_keyboard():
    return {
        "inline_keyboard": [
            [{"text": "🎲 Текст → 3D", "callback_data": "gen_text"}],
            [{"text": "🎨 Фото → 3D", "callback_data": "gen_photo"}],
            [{"text": "📦 История", "callback_data": "history"}],
            [{"text": "💎 Премиум", "callback_data": "premium"}]
        ]
    }


def premium_keyboard():
    return {
        "inline_keyboard": [
            [{"text": "💎 Купить Premium (299⭐)", "callback_data": "buy_premium"}],
            [{"text": "🔙 Назад", "callback_data": "menu"}]
        ]
    }


# ========== ГЕНЕРАЦИЯ 3D ==========
def generate_3d_text(prompt):
    headers = {"Authorization": f"Bearer {MESHY_API_KEY}", "Content-Type": "application/json"}
    payload = {"mode": "preview", "prompt": prompt, "art_style": "realistic", "should_remesh": True}
    resp = requests.post(MESHY_API_URL + "/text-to-3d", headers=headers, json=payload, timeout=30)
    if resp.status_code not in (200, 202):
        raise Exception(f"API error {resp.status_code}")
    task_id = resp.json().get("result")
    if not task_id:
        raise Exception("No task_id")
    start = time.time()
    while time.time() - start < 180:
        time.sleep(5)
        r = requests.get(MESHY_API_URL + f"/text-to-3d/{task_id}", headers=headers, timeout=30)
        if r.status_code == 200:
            data = r.json()
            if data.get("status") == "SUCCEEDED":
                model_url = data.get("model_urls", {}).get("glb")
                if model_url:
                    model_resp = requests.get(model_url, timeout=60)
                    if model_resp.status_code == 200:
                        return model_resp.content
            elif data.get("status") == "FAILED":
                raise Exception("Generation failed")
    raise Exception("Timeout")


def generate_3d_photo(image_base64):
    headers = {"Authorization": f"Bearer {MESHY_API_KEY}", "Content-Type": "application/json"}
    payload = {"image_base64": image_base64, "should_remesh": True}
    resp = requests.post(MESHY_API_URL + "/image-to-3d", headers=headers, json=payload, timeout=30)
    if resp.status_code not in (200, 202):
        raise Exception(f"API error {resp.status_code}")
    task_id = resp.json().get("result")
    if not task_id:
        raise Exception("No task_id")
    start = time.time()
    while time.time() - start < 180:
        time.sleep(5)
        r = requests.get(MESHY_API_URL + f"/image-to-3d/{task_id}", headers=headers, timeout=30)
        if r.status_code == 200:
            data = r.json()
            if data.get("status") == "SUCCEEDED":
                model_url = data.get("model_urls", {}).get("glb")
                if model_url:
                    model_resp = requests.get(model_url, timeout=60)
                    if model_resp.status_code == 200:
                        return model_resp.content
            elif data.get("status") == "FAILED":
                raise Exception("Generation failed")
    raise Exception("Timeout")


# ========== ОСНОВНОЙ ЦИКЛ ==========
def poll():
    last = 0
    print("🟢 Polling started")
    while True:
        try:
            resp = requests.get(API_URL + "/getUpdates", params={"offset": last + 1, "timeout": 30}, timeout=35)
            if resp.status_code == 200:
                for upd in resp.json().get("result", []):
                    last = upd["update_id"]
                    if "callback_query" in upd:
                        cb = upd["callback_query"]
                        chat = cb["message"]["chat"]["id"]
                        data = cb["data"]
                        requests.post(API_URL + "/answerCallbackQuery", json={"callback_query_id": cb["id"]})
                        if data == "menu":
                            send_message(chat, "Выбери действие:", keyboard=main_keyboard())
                        elif data == "premium":
                            send_message(chat, "💎 Премиум — 299 Telegram Stars", keyboard=premium_keyboard())
                        elif data == "buy_premium":
                            send_message(chat, "Скоро появится оплата через Stars")
                        elif data == "history":
                            send_message(chat, "📦 История появится позже")
                        elif data == "gen_text":
                            user_states[chat] = "text"
                            send_message(chat, "📝 Напиши описание модели:")
                        elif data == "gen_photo":
                            user_states[chat] = "photo"
                            send_message(chat, "📸 Отправь фото (JPG или PNG):")
                    elif "message" in upd:
                        msg = upd["message"]
                        chat = msg["chat"]["id"]
                        uid = msg["from"]["id"]
                        if "text" in msg and msg["text"] == "/start":
                            send_message(chat, "Создаю 3D-модели по тексту и фото\n🎁 3 модели/день бесплатно", keyboard=main_keyboard())
                            continue
                        if "text" in msg and user_states.get(chat) == "text":
                            del user_states[chat]
                            tier, left = get_user(uid)
                            if tier != "premium" and left <= 0:
                                send_message(chat, "❌ Лимит закончился. Купи Premium", keyboard=premium_keyboard())
                                continue
                            send_message(chat, "⏳ Генерирую 3D (1-2 мин)...")
                            try:
                                model = generate_3d_text(msg["text"])
                                if tier != "premium":
                                    decrement(uid)
                                send_document(chat, model, "model.glb", "✅ Готово!")
                            except Exception as e:
                                send_message(chat, f"❌ Ошибка: {str(e)}")
                        elif "photo" in msg and user_states.get(chat) == "photo":
                            del user_states[chat]
                            tier, left = get_user(uid)
                            if tier != "premium" and left <= 0:
                                send_message(chat, "❌ Лимит закончился. Купи Premium", keyboard=premium_keyboard())
                                continue
                            send_message(chat, "⏳ Обрабатываю фото (1-2 мин)...")
                            try:
                                file_id = msg["photo"][-1]["file_id"]
                                finfo = requests.get(API_URL + f"/getFile?file_id={file_id}").json()
                                if not finfo.get("ok"):
                                    raise Exception("File info error")
                                path = finfo["result"]["file_path"]
                                url = f"https://api.telegram.org/file/bot{TELEGRAM_BOT_TOKEN}/{path}"
                                img_data = requests.get(url).content
                                b64 = base64.b64encode(img_data).decode('utf-8')
                                model = generate_3d_photo(b64)
                                if tier != "premium":
                                    decrement(uid)
                                send_document(chat, model, "model.glb", "✅ Готово!")
                            except Exception as e:
                                send_message(chat, f"❌ Ошибка: {str(e)}")
                        else:
                            if "text" in msg:
                                send_message(chat, "Используй кнопки меню", keyboard=main_keyboard())
            time.sleep(1)
        except Exception as e:
            print(f"Poll error: {e}")
            time.sleep(5)


@app.route('/')
def home():
    return "Bot is online"


@app.route('/health')
def health():
    return "OK"


if __name__ == "__main__":
    import threading
    threading.Thread(target=poll, daemon=True).start()
    port = int(os.environ.get("PORT", 5000))
    app.run(host="0.0.0.0", port=port)